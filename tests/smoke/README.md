# VMNL Smoke Tests

Test policy lives in [docs/testing.md](../../docs/testing.md).

Smoke tests are executable entrypoints that validate minimal startup without opening a window.

```bash
./run -st
```
