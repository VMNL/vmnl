# Maintenance

These pages define the documentation contract, the API-change procedure, and generated-inventory checks. Run `just docs-api-check` before review; run `just docs-api-update` only after an intentional public-surface or documentation change.

