# Meshes — scaffolded

[`Mesh`](mesh.md) and [`MeshBuilder`](mesh_builder.md) can validate geometry and allocate Vulkan buffers. They cannot produce visible 3D rendering: any submitted 3D pass returns the explicit scaffold error.

